# 更新记录

## 未发布

## 1.0.11

- 修复连续快速删除多条监控时，后一次操作使用旧列表快照、导致前一条重新出现的问题。
- 设置改为成功落盘后再更新界面与监控引擎，避免 Windows 文件占用或权限异常时出现列表看似无法删除、重启后又恢复的状态分叉。
- 删除过程中显示明确的忙碌状态；删除失败时在监控列表旁直接提示，具体原因仍保留在活动日志中。

## 1.0.10

- 修复 Debian 安装包使用中文内部包名而无法安装的问题，并让 Debian / RPM 统一使用合法 ASCII 包名；公开资产改用稳定的 ASCII 文件名，发布流水线新增 DEB 实际安装和 Windows 安装启动验证。
- 单门店超过 20 个零件号时自动分批查询，Apple Watch 表带零件号也计入上限，避免 Apple 静默截断后遗漏型号。
- 中国大陆省、市、区只用于中国大陆站送货查询，不再污染香港、日本等其他地区的监控结果。
- macOS / Linux 设置目录和文件权限分别收紧为 `0700` / `0600`，读取旧配置时自动修复；Bark 网络与服务端错误不再暴露完整地址或设备密钥。
- Apple Watch 返回尚未开放的取货状态时显示“待开放取货”，保留 Apple 原始说明，不再误报为响应解析失败。
- 优化监控列表的状态、门店和型号列间距，窄窗口下仍保持内容清晰可读。
- 保护冷却期间新增“立即重试”，由用户自行决定是否跳过当前等待；暂停再开始后仍会保留真实冷却状态和重试入口。

## 1.0.9

- 正常查询成功时不再因累计请求额度逐轮延后；下一轮固定按用户设置的间隔开始，同时保留真实请求之间至少 2 秒的节流和 Apple 保护冷却。

## 1.0.8

- 应用正式使用中文名称「果到雷达」，并换用红色果实与雷达扫描组合的新图标；网页图标和各桌面平台安装包同步更新。
- 同一地区、相同型号组合的附近门店可复用首个取货响应；响应不完整时自动回退为目标门店查询。
- 新增取消安全的全局请求预算，按上一轮实际负载推迟下一轮，并在界面和日志中显示真实等待时间、请求次数与批量响应覆盖门店数。
- Apple 返回 `403`、`429` 或 `541` 后进入分级保护冷却，冷却期不再反复请求，结束后自动进行一次恢复探测。

## 1.0.7

- 补充北京荟聚 Apple Store（R792），北京门店列表由五家恢复为六家。

## 1.0.6

- 监控列表新增「预计送货」列，直接展示 Apple 返回的具体日期或日期范围，并按送达速度使用不同颜色；悬浮可查看 Apple 原始送货说明。
- 对 Apple 只返回「2–3 周」等范围的情况保留原文，不再推算并伪造成某个具体日期；同时提示可能因未选地址、商品组合或 Apple 暂未给出准确日期。
- 新增送货地区设置，省份、城市和区县选项直接读取 Apple 官网三级地址数据；选择区县后自动保存并用于后续送货查询，无需手工输入地址。
- 补齐 Apple Watch 送货组合：目录记录表壳、表带与组合零件号，监控时使用完整组合查询送货信息。
- Apple Watch 监控项支持按 Apple 官网选项选择表带款式、颜色和腕围尺码；同一表壳跨门店复用所选表带，并在列表、日志和提醒中显示实际表带信息。
- Apple Watch 型号名称改用结构化规格生成，恢复表壳材质、尺寸、连接类型和颜色等关键信息，避免营销文案覆盖铝金属、钛金属等材质。
- 有货状态下显示 Apple 返回的可取日期；列表表头「状态」改为「取货状态」，区分门店取货与送货信息。
- 优化桌面端自适应布局：窗口放大时中心区域同步伸展，加宽门店、预计送货和监控设置区域，长型号名称可获得更多显示空间。
- 恢复监控列表右侧删除按钮并固定操作列，横向内容较多时仍可直接删除监控项。
- 应用标题栏显示当前版本号，便于确认正在运行的构建版本。
- 扩充送货日期、送货地区、Apple Watch 组合、配置迁移和界面状态的自动化测试。

## 1.0.5

- 在应用顶部加入开源说明、GitHub 最新版下载入口和作者 X 主页入口。
- 为 GitHub 与 X 入口补充悬浮提示、键盘焦点和无障碍标签。

## 1.0.4

- 修复部分 Windows 电脑首次查询时永久卡住的问题：不再同步运行可能无法退出的 `chrome.exe --version`，改用受控的兼容 User-Agent 启动查询会话。
- 为 Chromium 页面目标、调试 WebSocket 和命令发送增加明确超时，连接异常时返回“未知”并继续后续重试，不再无限等待。

## 1.0.3

- 每个型号可以设置专属 Bark 地址；同一型号跨门店复用，并在未设置时沿用默认 Bark，便于把不同型号的到货提醒分发给不同的人。
- macOS 应用包改用完整的 ad-hoc 签名，并在发布流水线中验证应用包和 DMG 内的签名，修复从浏览器下载后被误报为“已损坏”的问题。

## 1.0.2

- 到货后可以选择不自动打开页面、打开 Apple 购物袋，或打开对应型号的商品详情。
- Bark 提醒链接与所选跳转方式保持一致；选择不自动打开时不附带页面链接。
- 监控列表中的型号名称可以直接点击，手动打开对应商品详情。
- 「测试提醒」升级为「测试提醒与跳转」，无需等待真实库存即可验证当前设置。
- 兼容旧版配置：原先开启自动打开购物袋的用户继续使用购物袋，关闭的用户继续保持不自动打开。
- Apple Watch 商品详情会进入对应系列配置页，继续选择表壳、尺寸与表带；所有跳转均不会自动加购、预留库存或下单。

## 1.0.1

- 型号目录从所选地区的 Apple 官网发现当前机型，新机无需逐代登记；更新七个地区的内置商品数据。
- 完整刷新成功后移除已撤下的机型页面；部分刷新失败时保留可用数据，并说明失败项目。
- 完善 Apple Watch 代数、尺寸和连接类型，以及 Mac、iPad 的芯片与配置显示；无法确认的规格不猜测补齐。
- iPhone 型号选择和监控列表优先显示新款。
- 区分有货、无货、不支持取货、暂未开售、即将发售与暂不可购买，各自使用独立配色。
- 每轮日志保留门店编号、商品零件号及取货、购买、配送说明；缺少数据或型号时保持未确认，避免误报无货。
- 更新界面显示累计下载进度、验证和安装阶段，补充超时、签名失败、重试与完整安装包入口。
- 提醒方式统一为提示音、Bark 和自动打开购物袋，移除旧的系统通知调用及相关权限。
- 补齐目录、型号规格、业务状态和更新提示测试，并更新 README 使用说明。

## 1.0.0

- 果到雷达首个正式版本，提供多门店、多型号监控、临时 Chromium 查询会话、逐轮日志、提示音和 Bark 提醒，以及自动打开购物袋。
